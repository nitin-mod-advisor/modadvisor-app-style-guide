/**
 * @fileOverview Firestore Security Rules for Color Palette Management
 *
 * Core Philosophy:
 * This ruleset prioritizes ease of prototyping. It allows public read access to color palettes
 * but requires authentication for any modification (create, update, delete). This allows
 * users to view palettes without signing in but prevents anonymous users from changing data.
 *
 * Data Structure:
 * Color palettes are stored in the `/palettes/{paletteId}` collection.
 *
 * Key Security Decisions:
 * - Public read access: Any user can read (get, list) color palettes.
 * - Authenticated writes: Only authenticated users can create, update, or delete color palettes.
 * - No data validation: During prototyping, the rules do not enforce strict schema validation
 *   beyond requiring authentication for write operations.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows public read access to color palettes, but requires authentication for writes.
     * @path /palettes/{paletteId}
     * @allow (get, list) Any user can read palettes.
     * @allow (create, update, delete) Authenticated users can modify palettes.
     * @deny (create, update, delete) Anonymous users cannot modify palettes.
     * @principle Allows public read access while restricting write access to authenticated users.
     */
    match /palettes/{paletteId} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update: if isSignedIn() && resource != null;
      allow delete: if isSignedIn() && resource != null;
    }

    // Helper function to check if the user is signed in.
    function isSignedIn() {
      return request.auth != null;
    }
  }
}