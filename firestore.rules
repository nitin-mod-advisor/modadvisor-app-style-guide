/**
 * @file Firestore Security Rules for Color Palettes.
 *
 * Core Philosophy:
 * This ruleset allows public read access to color palettes and restricts write access. There is currently no concept of user-specific color palettes, ownership, or roles.
 *
 * Data Structure:
 * All color palettes are stored in the top-level `/color_palettes/{colorPaletteId}` collection.
 *
 * Key Security Decisions:
 * - Color palettes are publicly readable.
 * - Write operations (create, update, delete) are disallowed for all users.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Controls access to individual color palettes.
     * @path /color_palettes/{colorPaletteId}
     * @allow (get, list): Any user can read any color palette.
     * @deny (create, update, delete): No user can create, update, or delete color palettes.
     * @principle Allows public read access but forbids write access.
     */
    match /color_palettes/{colorPaletteId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }
  }
}