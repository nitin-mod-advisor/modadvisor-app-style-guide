/**
 * @fileOverview Firestore Security Rules for Color Palette and Typography Management
 *
 * Core Philosophy:
 * This ruleset prioritizes ease of prototyping. It allows public read access to color palettes
 * and typography settings but requires authentication for any modification (create, update, delete).
 *
 * Data Structure:
 * - Color palettes: /palettes/{paletteId}
 * - Typography settings: /settings/typography
 *
 * Key Security Decisions:
 * - Public read access: Any user can read (get, list) palettes and settings.
 * - Authenticated writes: Only authenticated users can create, update, or delete data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * @description Allows public read access to color palettes, but requires authentication for writes.
     * @path /palettes/{paletteId}
     */
    match /palettes/{paletteId} {
      allow get, list: if true;
      allow create, update, delete: if isSignedIn();
    }

    /**
     * @description Allows public read of typography settings, but requires auth for writes.
     * The path is written to match a specific document, not a collection.
     * @path /settings/typography
     */
    match /settings/typography {
       allow get, list: if true;
       allow create, update, delete: if isSignedIn();
    }

    // Helper function to check if the user is signed in.
    function isSignedIn() {
      return request.auth != null;
    }
  }
}
